services:
  product_api:
    build: .
    container_name: product_api_container
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://database-1.c4xc68mao6ez.us-east-1.rds.amazonaws.com:3306/dbsu413
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
    restart: always